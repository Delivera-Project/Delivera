# Copia este archivo a .env.local y ajusta los valores según tu entorno.
# VITE_API_URL — URL base del backend (sin /api/v*).
VITE_API_URL=http://localhost:8080
# VITE_DEBUG — habilita logs detallados en desarrollo.
VITE_DEBUG=true
